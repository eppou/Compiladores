#ifndef LIBS_HPP
#define LIBS_HPP

// Include para operações de entrada e saída padrão
#include <iostream>

// Include para manipulação de strings
#include <string>

// Include para trabalho com vetores e listas
#include <vector>
#include <list>

// Include para manipulação de arquivos
#include <fstream>

// Include para manipulação de exceções
#include <stdexcept>

// Include para manipulação de tempo
#include <chrono>

// Include para threads e concorrência
#include <thread>

// Include para operações matemáticas
#include <cmath>

// Include para algoritmos de ordenação e busca
#include <algorithm>

// Include para estruturas de dados
#include <map>
#include <set>

// Include para programação orientada a objetos
#include <cstddef>

// Include para assert, usado para verificar condições
#include <cassert>

// Include para manipulação de caracteres e strings C
#include <cstring>

// Include para manipulação de ponteiros inteligentes
#include <memory>

#endif